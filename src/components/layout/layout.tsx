import React from "react";

const Layout = React.memo(
    ({
      children,
    } : {
      children: React.ReactNode;
    }) => {
      return (
        <div></div>
      )
    }
  );
  
  export default Layout;
  